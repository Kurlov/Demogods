# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /findMatch/:userId        controllers.Login.findMatch(userId: String)

POST        /gameState                controllers.Game.showGame